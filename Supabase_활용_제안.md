# Supabase로 더 할 수 있는 것 — 점검과 제안

> 2026-08-23 · 설정 동기화가 붙은 뒤, 같은 인프라로 무엇을 더 얹을 수 있는지 점검한 문서

---

## 0. 지금 쓰고 있는 것

| 기능 | 상태 |
|---|---|
| `device_settings` 표 + RPC 2개 | ✅ 배포됨 |
| 기기 간 설정 자동 동기화 | ✅ v77 |
| 스키마 GitHub Actions 자동 배포 | ✅ 성공 확인 |
| 7일 정지 방지 핑 | ✅ 매일 12:00 KST |

무료 플랜에서 지금 쓰는 양은 **500MB 중 1KB 미만**입니다. 앞으로 얹을 것들도 대부분 무료 한도 안에서 끝납니다.

---

## 1. 우선순위

| | 기능 | 왜 지금 | 난이도 | 비용 |
|---|---|---|---|---|
| **1** | **승인 기록 서버 보관** | 지금 겪는 문제 | 낮음 | 0 |
| **2** | **초안 백로그 큐** | 358편이 방치 중 | 중간 | 0 |
| **3** | **니치 쏠림 실측** | 사업 구조의 핵심 | 중간 | 0 |
| 4 | 글 성과 스냅샷 | 승인 후 필요 | 중간 | 0 |
| 5 | 라이선스 코드 | 판매 시작할 때 | 중간 | 0 |
| 6 | 워드프레스 CORS 프록시 | 고객이 생길 때 | 높음 | 0 |
| 7 | Realtime 즉시 반영 | 있으면 좋은 정도 | 낮음 | 0 |

---

## 2. 【1순위】 승인 기록 서버 보관

### 지금 뭐가 문제인가

진단 점수, 신청 이력, 반려 사유가 **전부 브라우저 localStorage**에 있습니다. 오늘만 해도 이 구조 때문에 두 번 막혔습니다 — PC에 Gemini 키가 없었고, 제가 테스트하다 워드프레스 비밀번호를 날렸습니다.

설정은 동기화로 해결됐지만 **기록은 여전히 기기에 묶여 있습니다.**

### 왜 중요한가

강경원님이 하려는 것이 이겁니다.

> "이 경험들을 스크린샷을 통해 기록해서 나중에 지식상품으로 판매하고 싶어"

`13/16 → 15/16`, `3회차 반려 → 무엇을 고쳤더니 4회차 승인` 같은 기록은 **한 번 잃으면 복원이 안 됩니다.** 그리고 고객이 생기면 이 기록이 그대로 **"우리는 평균 N회차에 승인시킵니다"** 라는 근거가 됩니다.

### 설계

```sql
create table public.approval_log (
  id          bigserial primary key,
  slot        text not null,              -- 기존 동기화와 같은 방식(암호문구 해시)
  kind        text not null,              -- check | apply | reject | approve | fix
  score       int,                        -- 진단 점수
  score_total int,
  payload     text,                       -- 세부는 암호화해서 보관
  note        text,                       -- 검색용 짧은 라벨(평문, 민감정보 금지)
  at          timestamptz default now()
);
```

`settings_get`/`settings_put`과 똑같이 **RPC 두 개**만 열면 됩니다. 이미 만든 패턴이라 복제에 가깝습니다.

앱에서는 승인 탭 「현황」에 **점수 추이 그래프**와 **회차별 타임라인**이 생깁니다.

---

## 3. 【2순위】 초안 백로그 큐

### 지금 뭐가 문제인가

**초안이 358편** 쌓여 있고, 공개는 48편입니다. 이 358편을 어떤 순서로 언제 내보낼지 관리하는 곳이 없습니다.

한꺼번에 풀면 그 자체가 자동 생성 신호가 되어 반려됩니다. 그렇다고 손으로 하루 한 편씩 고르는 건 주 14시간에서 감당이 안 됩니다.

### 설계

```sql
create table public.draft_queue (
  id         bigserial primary key,
  slot       text not null,
  wp_post_id bigint,                      -- 워드프레스 초안 ID
  title      text,
  category   text,
  intent     text,                        -- revenue | evergreen (strategy.py 분류)
  quality    int,                         -- 품질 점수
  release_at timestamptz,                 -- 예정 발행 시각
  status     text default 'queued',       -- queued | released | discarded
  created_at timestamptz default now()
);
```

`main.py`가 매일 실행될 때 **오늘 날짜의 `queued` 한 편을 꺼내 공개로 전환**합니다. `strategy.py`가 정한 요일별 의도 비율과 `ramp.py`의 단계별 발행량이 여기서 실제로 작동하기 시작합니다.

> 지금은 두 엔진이 계획만 세우고 **집행할 대상 목록이 없는 상태**입니다. 큐가 그 빈칸입니다.

---

## 4. 【3순위】 니치 쏠림 실측

### 지금 뭐가 문제인가

`niches.json`에 이런 값이 있습니다.

```json
"balancing": { "usage": { ... }, "penalty_per_user": 3, "soft_cap": 12 }
```

**`usage`가 하드코딩입니다.** 이용자가 몇 명이나 그 니치를 골랐는지 실제로 세지 않습니다. 지금은 이용자가 1명(본인)이라 문제가 없지만, 이 값이 가짜면 쏠림 방지가 작동하지 않습니다.

### 왜 중요한가

강경원님이 정한 원칙이 이겁니다.

> "이용자가 늘어갈수록, 개발자인 내가 파이가 없어지는 구조보다 내가 더 이득을 보는 구조로"

고객끼리 같은 니치·같은 키워드로 경쟁하면 **모두의 성과가 떨어지고 환불이 늘어납니다.** 반대로 실제 집계로 자연 분산시키면, 고객이 늘수록 데이터가 정확해지고 추천이 좋아집니다. **이게 복사되지 않는 자산입니다.**

### 설계

```sql
create table public.niche_usage (
  slot       text primary key,
  niche_key  text not null,
  region     text,
  decided_at timestamptz default now()
);

-- 집계는 개인을 식별하지 않는 숫자만 돌려준다
create or replace function public.niche_counts()
returns table(niche_key text, n bigint)
language sql security definer set search_path=public as $$
  select niche_key, count(*) from public.niche_usage group by 1;
$$;
```

앱은 니치를 정할 때 `niche_counts()`를 불러 **실제 인원수로 점수를 깎습니다.** 개인 정보는 나가지 않고 숫자만 오갑니다.

---

## 5. 그 다음 (승인 이후)

**글 성과 스냅샷** — 주간 Search Console 결과를 표로 저장하면 `insights.py`가 몇 주치가 아니라 **몇 달치 추이**를 보고 판단할 수 있습니다. 지금은 GitHub의 JSON 파일이라 누적 조회가 불편합니다.

**라이선스 코드** — 판매 시작할 때. 지금 구조(코드 검증이 브라우저 안에서만 일어남)는 5분이면 뚫립니다. 다만 정적 앱은 어차피 복사 가능하니 **정직한 고객이 결제하게 하는 수준**까지만 만들면 됩니다.

**워드프레스 CORS 프록시** — 고객 사이트의 보안 플러그인이 브라우저 요청을 막는 문제. 이미 겪으셨습니다. Edge Function이 중계하면 사라집니다. 고객이 여럿 생기면 CS 1순위가 될 항목입니다.

**Realtime** — 폰에서 바꾸면 PC에 즉시 반영. 지금은 60초 주기라 대부분 충분합니다. 급하지 않습니다.

---

## 6. 주의할 것

**무료 한도** — DB 500MB / Edge Functions 월 50만 회 / 대역폭 5GB. 위 기능을 다 얹어도 한동안 여유롭습니다. 이미지 같은 큰 파일만 넣지 않으면 됩니다.

**7일 정지** — 처리했습니다(매일 핑). 다만 **정지되면 조용히 실패**하므로, 핑 워크플로가 빨간불이면 알아채셔야 합니다.

**모두가 같은 프로젝트를 씁니다** — 앱에 주소·공개 키를 내장했으므로 고객도 이 DB를 씁니다. `slot`(암호문구 해시)으로 행이 분리되고 내용은 암호화되지만, **고객이 늘면 자기 프로젝트를 쓰게 하거나 Pro로 올려야 합니다.** 지금 판단할 일은 아닙니다.

**암호 문구를 잊으면 끝입니다** — 서버에 없으므로 복구 경로가 없습니다. 고객에게 팔 때는 이 경고를 결제 전에 보여줘야 합니다.

---

## 7. 제 권고

**지금은 1순위만 하시죠.** 승인 기록을 서버로 옮기는 것 — 오늘 겪은 문제의 연장이고, 지식상품의 원재료이며, 작업량도 작습니다(이미 만든 RPC 패턴 복제).

**2·3순위는 승인이 난 뒤**입니다. 초안 큐는 발행을 재개할 때 필요하고, 니치 집계는 두 번째 고객이 생겨야 의미가 있습니다.

지금 제일 중요한 건 여전히 **인테리어 29편 제목 정리 → 점검 통과 → 애드센스 신청**입니다. 인프라는 여기까지가 충분합니다.

---

### 미구현 항목

- [ ] `approval_log` 표 + RPC + 승인 탭 타임라인 (1순위)
- [ ] `draft_queue` 표 + `main.py` 연동 (2순위)
- [ ] `niche_usage` 표 + `niche_counts()` + 앱 반영 (3순위)
- [ ] 성과 스냅샷 / 라이선스 / CORS 프록시 / Realtime (이후)

### 관련 문서

- [`배포_아키텍처_결정.md`](배포_아키텍처_결정.md) — Vercel 대신 Supabase를 쓰는 이유
- [`_Master_Context/scripto_context.md`](../_Master_Context/scripto_context.md)
