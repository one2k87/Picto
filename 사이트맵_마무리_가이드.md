# 사이트맵 마무리 — 직접 하셔야 하는 2가지

> 2026-08-28 · 사이트맵 교체(Rank Math → 워드프레스 기본) 완료 후 남은 작업
> 자동으로 처리하려 했으나 **둘 다 Scripto/워드프레스 밖의 영역**이라 직접 하셔야 합니다.

---

## 지금 상태

| 항목 | 상태 |
|---|---|
| 새 사이트맵 `wp-sitemap.xml` | ✅ 정상 (글 29개, 죽은 링크 0) |
| 옛 사이트맵 `sitemap_index.xml` | ⚠️ 404 (Rank Math 모듈 껐음) |
| robots.txt의 Sitemap 줄 | ❌ 아직 옛 주소(404)를 가리킴 |
| Search Console 제출 | ❌ 아직 옛 사이트맵만 등록돼 있음 |

---

# 1. robots.txt 수정

## 왜 자동으로 못 했나

Rank Math의 robots.txt 편집기로 고쳐 저장했지만 **실제 파일은 바뀌지 않았습니다.** 캐시(Breeze·Varnish)를 비운 뒤에도 그대로입니다.

원인은 **서버에 실제 robots.txt 파일이 존재**하기 때문입니다. 물리 파일이 있으면 워드프레스가 만들어내는 robots.txt는 무시됩니다. Cloudways 공식 문서도 "SEO 플러그인으로는 robots.txt 편집이 막혀 있으니 파일로 처리하라"고 안내합니다.

## 현재 파일 내용

```
User-agent: *
Disallow: /wp-admin/
Allow: /wp-admin/admin-ajax.php

Sitemap: https://wontheland.com/sitemap_index.xml      ← 이 줄만 바꾸면 됩니다

#DaumWebMasterTool:b496b02e...
```

## 바꿔 넣을 내용 (전체를 이걸로 교체해도 됩니다)

```
User-agent: *
Disallow: /wp-admin/
Allow: /wp-admin/admin-ajax.php

Sitemap: https://wontheland.com/wp-sitemap.xml

#DaumWebMasterTool:b496b02e105734491791ebfcb5ccfdaa0433e2a4e8548cde9a62429a875cfe6a:+zpJ4W2W+Xhk6gqHHEwhTg==
```

> ⚠️ 마지막 다음 웹마스터도구 인증 줄은 **반드시 그대로 유지**하세요. 지우면 다음 검색 등록이 풀립니다.

## 방법 A — Cloudways 파일 관리자 (권장, 브라우저만으로)

1. `unified.cloudways.com` 로그인
2. 상단 **Servers** → 서버 선택
3. **🌐 www** 아이콘 클릭 → 해당 애플리케이션(wontheland) 선택
4. 왼쪽 **Application Management → File Manager** (또는 Deployment via Git 아래)
5. 경로 이동: `applications/[앱폴더명]/public_html`
6. `robots.txt` 우클릭 → **View/Edit**
7. 위 내용으로 교체 → **Save**

## 방법 B — SFTP (파일 관리자가 안 보일 때)

1. Cloudways → 서버 선택 → **Server Management → Master Credentials**
   여기에 SFTP 접속용 **Public IP / Username / Password**가 있습니다
2. FileZilla 등으로 접속 (Port 22, SFTP)
3. `/applications/[앱폴더명]/public_html/robots.txt` 다운로드 → 수정 → 업로드

> 비밀번호는 제가 대신 입력하지 않습니다. 직접 접속해 주세요.

## 확인

수정 후 브라우저에서 `https://wontheland.com/robots.txt` 를 열어 **`wp-sitemap.xml`** 로 바뀌었는지 봅니다.
바로 안 바뀌면 Cloudways에서 **Purge Varnish** 한 번 하고 다시 확인하세요.

---

# 2. Search Console에 새 사이트맵 제출

## 순서

1. [Search Console](https://search.google.com/search-console) 접속
2. 왼쪽 위에서 **속성(wontheland.com)** 선택
3. 왼쪽 메뉴 **색인 생성(Indexing) → Sitemaps(사이트맵)**
4. **"새 사이트맵 추가"** 칸에 아래를 입력하고 **제출**
   ```
   wp-sitemap.xml
   ```
   (도메인은 자동으로 붙습니다. 전체 주소를 넣어도 됩니다)
5. 기존에 등록돼 있는 **`sitemap_index.xml`** 항목을 클릭 → 우측 **⋮** → **사이트맵 삭제**
   - 지금은 404라서 그대로 두면 계속 "가져올 수 없음" 오류가 쌓입니다
   - 삭제해도 이미 색인된 페이지는 사라지지 않습니다

## 제출 후 확인 (바로가 아니라 하루 이틀 뒤)

| 볼 곳 | 정상 신호 |
|---|---|
| 사이트맵 → 상태 | **"성공"**, 발견된 URL 29개 안팎 |
| 색인 생성 → 페이지 | 색인된 페이지 수가 29에 가까워지는지 |
| 페이지 → 색인 생성되지 않음 | 404 항목이 더 늘지 않는지 |

> 색인 반영에는 며칠~2주가 걸립니다. **재신청은 색인 수가 안정된 뒤**에 하세요(발행 계획 기준 9/23 이후).

## 덤 — 초안으로 돌린 21편의 옛 주소

이미 구글이 색인해둔 주소가 있다면 이제 404가 됩니다. 이건 **정상이고 방치해도 됩니다** — 구글이 자연히 색인에서 제외합니다. Search Console의 "색인 생성되지 않음 → 찾을 수 없음(404)"에 21건이 뜨는 것은 오류가 아닙니다.

다만 그 글들을 **나중에 되살릴 계획이라면** 지금 지우지 말고 초안으로 두는 지금 상태가 맞습니다.

---

## 끝나면 알려주세요

두 가지가 끝나면 Scripto에서 전체 점검을 한 번 돌려 사이트맵 항목이 초록불로 바뀌는지 함께 확인하겠습니다.

### 출처

- [Cloudways: robots.txt 설정 방법](https://support.cloudways.com/en/articles/5124741-how-to-setup-robots-txt-for-your-website) — 파일 관리자·SFTP·SSH 세 가지 방법
- [Cloudways: 애플리케이션 캐시 비우기](https://support.cloudways.com/en/articles/8360096-how-to-purge-application-cache)
- [Search Console 사이트맵 제출(2026 인터페이스)](https://seo-hacker.com/google-search-console-guide-2026/) — 색인 생성 → Sitemaps
