# 성과·수익 실측 연동 설정 가이드

Scripto에 **실제 유입 데이터(Search Console·GA4)**와 **실제 수익(AdSense)**을 연결합니다.
전부 무료입니다. 크게 두 갈래예요:

- **① Search Console + GA4** → 서비스 계정 하나로 깔끔하게 연결 (필수 추천)
- **② AdSense 실측 수익** → 애드센스는 서비스 계정을 못 받아서 OAuth 토큰 방식(조금 더 복잡, 선택)

먼저 ①만 해도 "유입 검색어·상위 글 + 잘 되는 주제 자동 반영"이 켜집니다.
②는 나중에 해도 됩니다(안 하면 기존 예상치 유지).

---

## 1부. Search Console + GA4 (서비스 계정)

### STEP 1 — Google Cloud 프로젝트 만들기 + API 켜기

1. [console.cloud.google.com](https://console.cloud.google.com) 접속(구글 로그인).
2. 상단 프로젝트 선택 → **새 프로젝트** → 이름 `scripto` → 만들기.
3. 왼쪽 메뉴 **API 및 서비스 → 라이브러리**로 이동.
4. 아래 3개를 검색해 각각 **사용 설정(Enable)**:
   - `Google Search Console API`
   - `Google Analytics Data API`
   - `AdSense Management API` (②용, 지금 켜둬도 무방)

### STEP 2 — 서비스 계정 + 키(JSON) 만들기

1. **API 및 서비스 → 사용자 인증 정보(Credentials)**.
2. 상단 **+ 사용자 인증 정보 만들기 → 서비스 계정**.
3. 이름 `scripto-reader` → 만들고 계속 → 역할은 지정 안 해도 됨 → 완료.
4. 만들어진 서비스 계정을 클릭 → **키(Keys)** 탭 → **키 추가 → 새 키 만들기 → JSON** → 다운로드.
   - 이 JSON 파일이 열쇠입니다. **절대 공개 저장소·채팅에 올리지 말 것.**
5. 서비스 계정 화면에서 **이메일 주소**(예: `scripto-reader@scripto-xxxx.iam.gserviceaccount.com`)를 복사해 둡니다. 3·4단계에서 씁니다.

### STEP 3 — Search Console에 그 계정을 초대

1. [search.google.com/search-console](https://search.google.com/search-console) 접속.
2. (아직 사이트 등록 안 했으면) 내 블로그를 **속성 추가**로 등록 + 소유 확인.
3. 왼쪽 아래 **설정 → 사용자 및 권한 → 사용자 추가**.
4. 이메일에 **STEP 2의 서비스 계정 이메일** 붙여넣기 → 권한 **제한됨(Restricted)** → 추가.

> **사이트 주소(SC_SITE_URL) 형식 주의**
> - "URL 접두어" 속성이면: `https://내블로그.com/` (끝에 슬래시 포함, 정확히 일치해야 함)
> - "도메인" 속성이면: `sc-domain:내블로그.com`

### STEP 4 — GA4에 그 계정을 초대 + 속성 ID 확인

1. [analytics.google.com](https://analytics.google.com) → 내 GA4 속성 선택.
   (GA4를 아직 안 만들었으면 먼저 만들고 블로그에 측정 태그를 설치 — Rank Math/Site Kit로 연결 가능)
2. 왼쪽 아래 **관리(⚙️) → (속성 열) 속성 액세스 관리 → + → 사용자 추가**.
3. **서비스 계정 이메일** 입력 → 역할 **뷰어(Viewer)** → 추가.
4. 같은 **관리 → 속성 설정**에서 **속성 ID**(숫자 9자리 정도, 예 `481234567`)를 복사 → 이게 `GA4_PROPERTY_ID`.

### STEP 5 — GitHub Secrets에 입력

저장소 → **Settings → Secrets and variables → Actions → New repository secret** 로 아래를 추가:

| 시크릿 이름 | 값 |
|---|---|
| `GOOGLE_SERVICE_ACCOUNT_JSON` | STEP 2에서 받은 JSON **파일 내용 전체**(통째로 붙여넣기) |
| `SC_SITE_URL` | STEP 3의 사이트 주소 형식 (`https://내블로그.com/` 또는 `sc-domain:내블로그.com`) |
| `GA4_PROPERTY_ID` | STEP 4의 속성 ID(숫자) |

### STEP 6 — 테스트

1. GitHub → **Actions → 매일 블로그 자동 생성 → Run workflow**로 수동 1회 실행
   (또는 앱의 ▶ 버튼).
2. 실행 로그에 `[insights] Search Console: 검색어 … · 페이지 …`, `[insights] GA4: 상위 페이지 …`가
   보이면 성공.
3. 앱을 🔄 하면 상단에 **📈 성과·수익 패널**이 뜹니다.

> 데이터는 며칠 쌓여야 의미가 생깁니다. 갓 만든 사이트면 처음엔 비어 있을 수 있어요.

---

## 2부. AdSense 실측 수익 (OAuth, 선택)

애드센스는 **서비스 계정을 사용자로 추가할 수 없어서** 계정 주인이 직접 한 번 승인하는
OAuth 방식이 필요합니다. 한 번만 하면 이후 자동으로 갱신됩니다.

### STEP A — OAuth 클라이언트 만들기

1. Google Cloud → **API 및 서비스 → OAuth 동의 화면** → 유형 **외부** → 앱 이름/이메일만 채워 저장.
   - 테스트 사용자에 **내 구글 계정(애드센스 계정)** 을 추가.
2. **사용자 인증 정보 → + 만들기 → OAuth 클라이언트 ID → 데스크톱 앱** → 만들기.
3. 나오는 **클라이언트 ID**와 **클라이언트 보안 비밀**을 복사.

### STEP B — 리프레시 토큰 1회 발급

컴퓨터 터미널에서(파이썬 필요):

```
pip install google-auth-oauthlib
python3 -c "
from google_auth_oauthlib.flow import InstalledAppFlow
f=InstalledAppFlow.from_client_config(
 {'installed':{'client_id':'클라이언트ID','client_secret':'보안비밀',
  'auth_uri':'https://accounts.google.com/o/oauth2/auth',
  'token_uri':'https://oauth2.googleapis.com/token'}},
 ['https://www.googleapis.com/auth/adsense.readonly'])
c=f.run_local_server(port=0)
print('REFRESH TOKEN:', c.refresh_token)"
```

- 브라우저가 열리면 **애드센스 계정으로 로그인 → 허용**.
- 터미널에 찍히는 `REFRESH TOKEN: 1//...` 값을 복사.

### STEP C — GitHub Secrets에 추가

| 시크릿 이름 | 값 |
|---|---|
| `GOOGLE_OAUTH_CLIENT_ID` | STEP A의 클라이언트 ID |
| `GOOGLE_OAUTH_CLIENT_SECRET` | STEP A의 보안 비밀 |
| `ADSENSE_REFRESH_TOKEN` | STEP B의 리프레시 토큰 |

다음 실행부터 앱 성과 패널에 **실제 애드센스 수익**이 표시됩니다.

---

## 자주 막히는 곳

- **403 / 권한 오류**: 서비스 계정 이메일을 Search Console·GA4에 **초대**했는지 확인.
  API를 Cloud에서 **사용 설정**했는지도 확인.
- **Search Console 빈 결과**: `SC_SITE_URL` 형식이 속성과 정확히 일치하는지(슬래시/도메인 형식).
- **GA4 빈 결과**: 속성 ID(숫자)가 맞는지, 데이터가 아직 쌓였는지.
- **JSON 시크릿**: 줄바꿈 포함 파일 내용을 **그대로 전부** 붙여넣어야 함(따옴표 추가 금지).
- **보안**: 서비스 계정 JSON·리프레시 토큰은 GitHub Secrets에만. 코드/채팅에 붙이지 말 것.

막히면 그 화면(또는 로그의 `[insights] … 실패` 줄)을 알려주세요. 바로 짚어드리겠습니다.
