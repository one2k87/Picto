# 배포 아키텍처 결정 — Vercel로 옮길까?

> 2026-08-23 · 질문: "Vercel 같은 곳에 올리고 도메인을 받을까? 거기서 키를 설정하는 게 나을까?
> 나중에 다른 이용자에게 코드를 나눠주는 시스템까지 생각하면?"

---

## 0. 결론

| 항목 | 판단 |
|---|---|
| **키를 서버에 두기** | ❌ 하지 마세요. 사업 구조가 무너집니다 |
| **Vercel로 이전** | ⏸ 지금은 아닙니다. 필요해지면 Supabase Edge Functions가 먼저입니다 |
| **커스텀 도메인** | ✅ 하세요. 단 **GitHub Pages에 붙이면 무료**입니다 |
| **코드(라이선스) 시스템** | ✅ 필요합니다. 다만 Vercel이 아니라 **Supabase**로 충분합니다 |

---

## 1. "Vercel에서 키를 설정한다"가 왜 안 되는가

먼저 짚을 것: **Vercel에 정적 파일을 올리는 것은 GitHub Pages와 차이가 없습니다.** 키 문제는 전혀 달라지지 않습니다. 브라우저에 내려간 파일 안의 값은 누구나 볼 수 있습니다.

Vercel이 다른 점은 **서버리스 함수**를 돌릴 수 있다는 것뿐입니다. 거기에 키를 두면 브라우저에 노출되지 않습니다. 그런데 그 키가 **누구 키인가**에 따라 두 갈래로 갈리고, 둘 다 막힙니다.

### ① 내 키를 서버에 둔다

이용자가 늘수록 **내가 모든 AI 비용을 냅니다.**

| 이용자 | 월 AI 비용(추정) | 구독료 수입 | 남는 것 |
|---|---|---|---|
| 10명 | 내가 부담 | 정액+구독 | 마진 있음 |
| 100명 | 내가 부담 | 정액+구독 | **마진이 얇아짐** |
| 1,000명 | 내가 부담 | 정액+구독 | **쓸수록 손해나는 이용자 발생** |

무제한으로 쓰는 소수가 전체 마진을 먹습니다. 이걸 막으려면 사용량 제한·과금 정산을 붙여야 하는데, 그 순간 1인 개발로 감당할 수 없는 시스템이 됩니다.

`solvup_부업_후보_구상안.md`에 이미 적어둔 원칙입니다.

> BYOK(고객이 자기 AI 키 사용) — **고객이 늘어도 마진이 안 깎이는 유일한 구조**

이건 타협하면 안 되는 부분입니다.

### ② 이용자 키를 서버에 둔다

남의 자격 증명을 내 서버에 보관하는 겁니다.

- 유출되면 **내 책임**입니다. 워드프레스 비밀번호까지 함께 있으면 피해 규모가 큽니다
- 이것도 이미 정한 원칙에 어긋납니다 — *"타인 계정 비밀번호를 다루는 자동화는 만들지 않는다"*
- 크몽 대행업체들이 하는 방식인데, 강경원님이 그게 싫어서 직접 만들기 시작한 겁니다

### 그래서 키는 이용자 브라우저에 둡니다

지금 방식이 맞습니다. **오늘 만든 Supabase 동기화가 이 구조의 마지막 조각**입니다 — 키는 기기에 있고, 서버에는 암호문만 올라가고, 열쇠는 이용자만 가집니다. 기기를 옮겨도 다시 입력할 필요가 없어졌습니다.

---

## 2. Vercel의 실제 비용

| 플랜 | 가격 | 상업적 이용 |
|---|---|---|
| Hobby | 무료 | ❌ **불가** — 개인·비상업 전용 |
| Pro | **$20/월** (약 29,000원) | ✅ 가능 |

**돈을 받는 순간 Pro가 강제됩니다.** 매출이 얼마든 상관없습니다.

지금 고정비가 월 22,000원(Cloudways $14 + 도메인)인데 여기에 29,000원이 붙으면 **월 5만원**이 됩니다. 수익이 0인 상태에서 두 배 이상으로 올리는 선택입니다.

---

## 3. 그럼 백엔드가 아예 필요 없는가 — 아닙니다

코드를 나눠주는 시스템을 만들려면 **서버가 반드시 필요한 지점**이 있습니다.

| 필요한 것 | 왜 브라우저로는 안 되나 |
|---|---|
| **라이선스 코드 검증** | 지금은 브라우저 안에서만 확인합니다. 개발자 도구로 5분이면 뚫립니다 |
| **결제 웹훅 수신** | Gumroad·Lemon Squeezy가 결제 완료를 서버로 알려줘야 코드가 발급됩니다 |
| **워드프레스 CORS 우회** | 고객 사이트의 보안 플러그인이 브라우저 요청을 막습니다. 실제로 이미 겪은 문제입니다 |
| **고객별 예약 실행** | 지금은 제 GitHub Actions가 돕니다. 고객은 그걸 못 씁니다 |

**이걸 Vercel 없이 Supabase만으로 다 할 수 있습니다.**

| | Supabase 무료 | 비고 |
|---|---|---|
| Edge Functions | **월 50만 회** | 라이선스 검증·웹훅·CORS 프록시 전부 가능 |
| DB | 500MB | 코드·고객·사용량 기록 |
| Auth | MAU 5만 | 필요해지면 |
| pg_cron | 있음 | 고객별 예약 실행 |

이미 Supabase를 붙여놨으니 **플랫폼을 하나 더 늘릴 이유가 없습니다.** Vercel을 추가하면 배포 대상이 둘, 시크릿 관리가 둘, 장애 지점이 둘이 됩니다.

> ⚠️ **무료 플랜 함정**: Supabase는 **7일간 요청이 없으면 프로젝트를 정지**시킵니다.
> 정지되면 대시보드에서 수동으로 깨워야 합니다. 이미 돌고 있는
> `weekly-strategy.yml`(매주 월요일)이 DB를 건드리게 해두면 자연스럽게 방지됩니다.
> 유료 고객을 받기 전에 반드시 처리해야 할 항목입니다. *(미구현)*

---

## 4. 도메인은 하세요 — 단, GitHub Pages에

`one2k87.github.io/Scripto` 는 취미 프로젝트로 보이고, `scripto.kr` 은 제품으로 보입니다. 돈을 받을 거라면 이 차이가 큽니다.

**GitHub Pages는 커스텀 도메인을 무료로 지원하고 HTTPS 인증서도 자동으로 붙습니다.** Vercel로 옮길 이유가 되지 못합니다.

### 방법

1. 도메인 구입 (`.kr` 22,000원 / `.com` 23,500원) → **자동 갱신 켜기**
2. DNS에 레코드 추가

| 타입 | 호스트 | 값 |
|---|---|---|
| A | `@` | `185.199.108.153` |
| A | `@` | `185.199.109.153` |
| A | `@` | `185.199.110.153` |
| A | `@` | `185.199.111.153` |
| CNAME | `www` | `one2k87.github.io` |

3. GitHub 저장소 → Settings → Pages → Custom domain에 입력
4. **Enforce HTTPS** 체크 (인증서 발급까지 몇 분~한 시간)

> 도메인 이름은 애드센스 블로그와 겹치지 않게 하세요. Scripto는 도구이고 wontheland는 콘텐츠 사이트라, 같은 브랜드로 묶으면 나중에 둘 다 애매해집니다.

---

## 5. 라이선스 코드 시스템 설계 (필요해질 때)

Supabase만으로 만드는 구조입니다.

```
결제(Gumroad/Lemon Squeezy)
   │  웹훅
   ▼
Supabase Edge Function  ──▶  licenses 표에 코드 발급
                                slot / plan / expires_at / seats
   ▲
   │  코드 입력 시 검증 요청
브라우저(Scripto)  ──▶  플랜 잠금 해제
```

**표 설계 초안**

```sql
create table public.licenses (
  code        text primary key,          -- 고객에게 주는 코드
  plan        text not null,             -- basic | pro | max
  status      text not null default 'active',
  issued_at   timestamptz default now(),
  expires_at  timestamptz,
  device_hash text,                      -- 첫 사용 기기에 묶어 공유 방지
  note        text
);
```

검증은 **security definer 함수 하나**로 끝납니다. 오늘 만든 `settings_get` / `settings_put`과 똑같은 방식입니다 — 공개 키로 함수만 호출하고, 표에는 직접 접근하지 못하게 합니다.

### 다만, 과투자하지 마세요

**정적 앱은 통째로 복사할 수 있습니다.** 서버 검증을 붙여도 마음먹은 사람은 우회합니다. 방어에 시간을 많이 쓰는 건 손해입니다.

이미 내린 결론이 여기서도 유효합니다.

> 정보는 복사되지만 **실행·누적·집계 데이터·책임**은 복사되지 않는다.

코드 시스템은 **정직한 고객이 결제하도록 하는 장치**면 충분합니다. 완벽한 DRM은 목표가 아닙니다.

---

## 6. 언제 무엇을 할지

| 시점 | 할 일 | 비용 |
|---|---|---|
| **지금 (승인 전)** | 아무것도 바꾸지 않음. 승인에 집중 | 0원 |
| 승인 직후 | 커스텀 도메인 → **GitHub Pages** | 22,000원/년 |
| 첫 고객 직전 | 라이선스 검증 → **Supabase Edge Function**<br>Supabase 정지 방지 크론 | 0원 |
| 고객 10명 넘을 때 | 워드프레스 CORS 프록시 → Edge Function | 0원 |
| Supabase 무료 한도 초과 | Supabase Pro | $25/월 |
| **Next.js가 정말 필요할 때만** | Vercel Pro | $20/월 |

마지막 줄은 아마 오지 않습니다. Scripto는 단일 파일 앱이고, 그 구조가 오히려 강점입니다 — 배포가 git push 하나로 끝나고, 장애 지점이 없습니다.

---

## 7. 한 줄 요약

> **Vercel은 키 문제를 해결하지 못합니다.** 정적 호스팅은 GitHub Pages와 같고,
> 서버에 키를 두는 순간 BYOK 구조가 무너집니다.
>
> 도메인은 **GitHub Pages에 무료로** 붙이고, 백엔드가 필요해지면
> **이미 있는 Supabase**를 쓰세요. 플랫폼은 하나로 유지하는 게 낫습니다.

---

### 미구현 항목

- Supabase 7일 정지 방지 (기존 워크플로에 DB 핑 추가)
- 라이선스 표 + 검증 Edge Function
- 결제 웹훅 수신 Edge Function
- 워드프레스 CORS 프록시 Edge Function
- 커스텀 도메인 연결

### 출처

- [Vercel Hobby 플랜 — 비상업 전용](https://vercel.com/docs/plans/hobby)
- [Vercel 요금제](https://vercel.com/pricing)
- [Supabase 무료 플랜 한도 (2026)](https://automationatlas.io/answers/supabase-free-tier-limits-2026/)
