# Scripto — 수익 파이프라인 설계 · 기능 백로그 · 보안/안정성 감사

> 2026-08-31 · 실제 코드베이스와 운영 데이터를 근거로 작성. 일반론이 아니라 이 저장소 기준.

---

## 1. 데이터로 만드는 수익 파이프라인 — 7가지 방안

핵심 자산은 코드가 아니라 **"반려→수정→결과" 인과 데이터와 "키워드→트래픽→수익" 성과 데이터**다. 이 둘은 이용자가 늘수록 스스로 좋아지고, 경쟁자가 돈으로 못 사는 것.

| # | 방안 | 데이터 근거 | 시점 |
|---|---|---|---|
| 1 | **월 구독 (핵심)** — 자동 발행+승인 엔진+결산. 승인 전 무료/저가 → 승인 후 유료 전환이 자연스러움("승인시켜준 도구"라는 신뢰가 결제 전환점) | 승인 여정 전체 | P3 |
| 2 | **초기 세팅비** — 도메인·WP·플러그인·필수 페이지·소개까지 원클릭 대행. 이미 러너로 자동화됨 → 원가 거의 0 | 세팅 러너 | P3 |
| 3 | **승인 보험형 요금** — "N주 내 미승인 시 다음 달 무료". 반려 데이터가 쌓여 승인률을 예측할 수 있어야 가능 → 데이터 모트가 곧 가격 정책 | rejections 데이터셋 | P4 |
| 4 | **키워드 성과 리포트/앱** — "이 니치에서 실제 승인·클릭으로 이어진 키워드" 월간 리포트 판매 또는 앱 내 상위 플랜 기능. 네이버/구글 키워드 도구는 검색량만 알지 '승인에 통했는가'는 모름 | keyword_perf | P4 |
| 5 | **호스팅/도메인 제휴 → 대행** — 도메인 마법사에서 제휴 링크(Cloudways 등 어필리에이트)로 시작, 이용자가 늘면 리셀러/자체 프로비저닝 마진 | 마법사 퍼널 | P1→P4 |
| 6 | **지식상품** — 강의·전자책. 이미 전자책 1종 + 강의자료 촬영 진행 중. "6번 반려→승인 실측 기록"이 콘텐츠 차별점 | 승인 실전 기록 | 즉시 가능 |
| 7 | **자체 블로그 포트폴리오** — 도구를 자기 블로그 여러 개에 돌려 애드센스+제휴 수익 자체를 파이프라인화(도구 검증 겸용) | 전체 | 승인 후 |

**우선순위 제안**: 6(즉시, 재료 있음) → 1·2(승인 실적 1건 확보 후) → 5 제휴 → 3·4(이용자 데이터 임계량 후). 데이터 수집(Supabase 적재)은 이용자 1호(본인)부터 시작해야 3·4가 가능해지므로 P1에서 스키마 확정이 중요.

---

## 2. 있었으면 하는 기능 백로그 (로드맵 외 추가 제안)

**승인 엔진 쪽**
- 반려 사유 분류기: 자유 텍스트 반려 문구 → 표준 코드(low_value/policy/navigation…) 자동 분류. 지금은 정규식, 데이터 쌓이면 학습.
- 신청 전 "심사관 시뮬레이션": 방문자 시점 크롤(이미 있음)에 + 모바일 뷰·CLS·깨진 링크 검사 추가.
- 색인 상태 보드: URL Inspection API 연동 후 글별 색인/크롤 날짜 신호등.

**콘텐츠 쪽**
- 내부링크 자동 재배선: 새 글 발행 시 관련 옛 글 2~3곳에 링크 삽입(클러스터 강화). 부분 구현(#33) 확장.
- 성공 글 역학습: 결산의 상위 클릭 글 제목·구조 특징을 era/프롬프트에 반영(수익탭 P2와 연결).
- 원본 사진 모바일 플로우: 폰에서 사진 찍어 업로드→글에 자동 배치(Experience 신호 중 자동화 불가 항목의 마찰 최소화).
- 발행 전 미리보기 카드: 예약 글을 모바일에서 스와이프로 승인/보류(구독 제품의 핵심 UX).

**운영 쪽**
- 텔레그램 요약 알림 확장: 매일 생성 결과+결산 요약 푸시(notify.py 기존 인프라 활용).
- 설정 내보내기/가져오기: CFG 백업 파일(암호화) — 기기 변경·복구 대비.
- 멀티 사이트 전환기: acctKey 스냅샷은 있음 → 계정 전환 UI로 승격.

---

## 3. 보안 감사 — 실측 결과

### 양호 (설계 의도대로)
- **BYOK 원칙 일관**: Gemini 키·WP 앱 비밀번호·GitHub PAT가 전부 사용자 브라우저 localStorage(CFG)에만 있고 서버로 안 나감. Supabase publishable key만 공개(의도), service_role은 Actions 시크릿에만.
- **PAT 권한이 이미 최소 범위에 가까움**: 실측 — contents 읽기/쓰기·workflow dispatch 가능, **secrets 접근 403**(오늘 확인). 시크릿 탈취 불가.
- **syncPass 클라이언트 유지** — 서버 전송 없음.
- 필수 페이지 생성기에 플레이스홀더 금지 가드(과거 사고 재발 방지 주석 존재).

### 개선 권장 (우선순위순)
1. **localStorage의 비밀값 = XSS 한 방 거리**. 대시보드는 innerHTML 조립이 많고 escAttr이 대부분 적용돼 있으나, WP 글 제목 등 외부 유래 문자열 경로가 많아 전수 보장이 어렵다. 권장: ①비밀값(CFG.pass/llmKey/ghToken)을 WebCrypto로 감싸 저장(열쇠는 세션 파생) ②CSP 메타 태그 추가(script-src 'self' + 허용 CDN만) — GitHub Pages에서도 meta로 가능. **구독 제품 전환 전 필수.**
2. **git remote URL에 PAT 평문 포함**(로컬 `.git/config`). 백업·화면공유로 샐 수 있는 경로. 권장: `git remote set-url origin https://github.com/one2k87/Scripto.git` + macOS 키체인 credential helper. (로컬 작업 파일이라 원격 노출은 아님)
3. **워드프레스 앱 비밀번호 권한**: 현재 관리자 계정 발급으로 추정 — 편집자(Editor) 권한 전용 계정으로 재발급하면 유출 시 피해 반경이 글/페이지로 제한됨(플러그인·사용자 관리 불가).
4. **functions.php robots 필터**: 테마 업데이트 시 소실(주석 있음). Cloudways 재작성 규칙이 본선이므로 위험은 낮으나, 차일드 테마로 옮기면 소실 자체가 없어짐.
5. 이용자 데이터 수집(P1) 때: 사이트 URL 해시화, 반려 문구 원문에서 개인 식별 정보 스트립, 옵트인 — 스키마에 이미 반영해둠(로드맵 문서).

### 안정성 감사
- **잘된 것**: 작업 재개(jobs/doneIds), 계정별 스냅샷, verifyFix 비교식 검증, quality 게이트, Supabase keepalive, 이번에 추가한 image_errors 관측.
- **남은 취약 지점**:
  - *단일 Gemini 키 쿼터 공유* — 브라우저 도구와 CI가 같은 키. 대량 작업 날 무료 한도(이미지 계열이 특히 타이트) 충돌 가능. 권장: CI용 키 분리(무료 키 2개), images.py에 429 시 지수 백오프(현재 1회 실패 즉시 포기).
  - *Actions 결과 커밋 경쟁* — bot 커밋과 수동 push가 자주 충돌(오늘 2회 rebase). 권장: 워크플로 커밋에 `git pull --rebase` 선행(1줄).
  - *GitHub Pages 배포 지연* — 배포 후 1~2분 캐시. 대시보드에 버전 문자열 표시 + 구버전 감지 시 새로고침 안내 배너 권장.
  - *quality 게이트 통과율* — 현재 하루 1편 생성 중 상당수 폐기. 관측은 붙었으니 2주 폐기 사유 통계로 프롬프트 보정(임계값은 실측으로 원칙 그대로).
  - *localStorage 용량* — 스냅샷+결산 스냅샷 추가로 증가 추세(현재 ~50KB 수준, 한도 5MB). digestSnaps 180일 캡 적용해둠. CFG 총량 표시를 설정 탭에 추가 권장.

---

## 4. 이번에 구현된 것 (v122)

- 📆 결산: 주(7일)/월(30일) 발행 수·직전 대비·유형 구성(트/스/에), SC 클릭·노출·GA 조회 **일일 스냅샷 시계열**(180일 보관) → 7일 변화 표시.
- 🎯 비율 권장: 승인 전 = 규칙(10/25/65, 근거 표시) → 클릭 표본 50+ = 실측(유형별 글당 평균 클릭) 절반 가중 자동 보정. 근거 문구가 항상 함께 표시됨.
- 원클릭 적용: 저장소 `data/site_categories.json`의 `mix`에 기록 → 매일 생성이 날짜 시드로 오늘의 주제 유형(트렌드=season 레인 / 스테디·에버그린=long 레인+지시문)을 뽑는다. 하루 1편이어도 주 단위로 비율에 수렴.
